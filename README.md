# multimodal-rag-document-intelligence
Built a Multi-Modal RAG system that processes PDFs, scanned images (using OCR), and text documents to answer user queries with high accuracy. Implemented a full pipeline—OCR, chunking, embeddings, vector search (FAISS/Chroma), and LLM-based answer generation—with a simple UI for interactive retrieval.
